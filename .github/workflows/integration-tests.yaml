name: Integration Tests
env:
  AWS_DEFAULT_REGION: us-east-1
  AWS_DEFAULT_OUTPUT: text
on:
  push: {branches: [main]}
  pull_request: {branches: [main]}
jobs:
  eks:
    outputs:
      cluster-name: ${{ steps.create-cluster.outputs.cluster-name }}
      oidc-arn: ${{ steps.oidc.outputs.arn }}
      oidc-issuer: ${{ steps.oidc.outputs.issuer }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - id: create-cluster
      env:
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      run: echo "::set-output name=cluster-name::my-cluster"
      #  eksctl create cluster --name aws-iam-controller-integration-${{ github.run_number }}
      #  echo "::set-output name=cluster-name::aws-iam-controller-integration-${{ github.run_number }}"
    - id: oidc
      env:
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      run: |
        issuer=$(aws eks describe-cluster --name ${{ steps.create-cluster.outputs.cluster-name }} --query "cluster.identity.oidc.issuer" | sed 's/https:\/\///g')
        arn="arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:oidc-provider/${issuer}"
        echo "::set-output name=arn::${arn}"
        echo "::set-output name=issuer::$issuer"
  terraform:
    timeout-minutes: 2
    outputs:
      role-arn: ${{ steps.role-arn.outputs.role-arn }}
    needs: [eks]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    strategy:
      fail-fast: false
      matrix:
        go-version: [ "1.17" ]
    steps:
    - uses: actions/checkout@v2
    - uses: hashicorp/setup-terraform@v1
    - run: terraform init
    - env:
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      run: |
          terraform import -var=account_id=${{ secrets.AWS_ACCOUNT_ID }} -var=oidc-issuer=${{ needs.eks.outputs.oidc-issuer }} aws_iam_role.aws_iam_controller aws-iam-controller || true
          terraform apply -auto-approve -var=account_id=${{ secrets.AWS_ACCOUNT_ID }} -var=oidc-issuer=${{ needs.eks.outputs.oidc-issuer }}
    - run: echo "::set-output name=role-arn::$(terraform output -raw role-arn)"
      id: role-arn
  integration:
    needs: [terraform,eks]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go-version: [ "1.17" ]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go-version }}
      - uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}
      - run: make docker-build && make docker-push
      - run: aws eks update-kubeconfig --name ${{ needs.eks.outputs.cluster-name }}
        env:
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      - run: kubeflow get namespaces
        env:
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      - run: make deploy
        env:
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  cleanup:
    if: always()
    needs:
    - terraform
    - integration
    - eks
    runs-on: ubuntu-latest
    steps:
    - run: |
        eksctl delete cluster ${{ needs.eks.outputs.cluster-name }}-1 || true
    - working-directory: terraform
      run: |
        terraform import -var=account_id=${{ secrets.AWS_ACCOUNT_ID }} -var=oidc-issuer=${{ needs.eks.outputs.oidc-issuer }} aws_iam_role.aws_iam_controller aws-iam-controller || true
        terraform destroy -auto-approve -var=account_id=${{ secrets.AWS_ACCOUNT_ID }} -var=oidc-issuer=${{ needs.eks.outputs.oidc-issuer }}
